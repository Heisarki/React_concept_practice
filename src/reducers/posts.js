export default posts = (store = [], action)=>{
    switch(action.type) {
        case "case1":
            return store;
        case "case2":
            return store;
        case "default":
            return store;
    }
    }

